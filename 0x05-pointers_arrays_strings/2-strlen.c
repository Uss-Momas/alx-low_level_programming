#include "main.h"
#include <string.h>
/**
  * _strlen - function thar returns the length of a string
  * @s: is the String
  * Return: the length of the @s
  */
int _strlen(char *s)
{
	return (strlen(s));
}
