#include "main.h"
#include <string.h>
/**
  * _strcpy - copies a string poitend by src to another destination, the *dest
  * @dest: the destination of the copied string
  * @src: the string that holds the location of the string characters
  * Return: a char
  */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
